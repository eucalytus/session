version: 2

jobs:
  build:
    docker:
      - image: golang:1.12
    working_directory: /go/src/github.com/eucalytus/session
    steps:
      - checkout
      - run:
          name: "Build & Test"
          command: go test